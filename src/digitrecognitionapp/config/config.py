# Configuration file for the digit recognition app
# This file contains the configuration settings for the app, including
# batch size, number of epochs, learning rate, and saved path.
configuration = {
    "batch_size": 128,
    "n_epoch": 10,
    "learning_rate": 1e-4,
    "saved_path": "version_1",
}
